{
  "name": "scraper-api",
  "version": "1.0.0",
  "description": "REST API scraper: got (HTTP/2) + Playwright fallback",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2",
    "got": "^11.8.6",
    "playwright": "^1.48.0"
  }
}